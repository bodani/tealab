[Unit]
Description=Memcached 
Before=httpd.service
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/memcached_exporter --memcached.address localhost:{{ MEMCACHED_PORT }}

[Install]
WantedBy=multi-user.target